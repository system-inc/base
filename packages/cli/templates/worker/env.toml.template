# Per-worker environment variables, keyed by environment. This file is
# gitignored — put secrets and per-environment config here without committing
# them. The `base` CLI loads the section matching the active environment.
#
# Example:
#   [Development]
#   DATABASES = [{ name = "@default", url = "mysql://..." }]
#   MY_SECRET = "..."

[Development]

[Staging]

[Production]
